<template name="starTemplate">
  <view class='star-wrap'>
    <block wx:for="{{stars}}" wx:for-item="star" wx:key='index'>
      <image src="{{star ? '/icons/collected.png' : '/icons/no-star.png' }}"></image>
    </block>
    <text class='score'>{{score}}</text>
  </view>
</template>